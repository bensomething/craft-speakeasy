# Release Notes for Sesame

## Unreleased

### Added
- Initial scaffold: **Sesame Password** encrypted field type (works on any
  element type), front-end gating with an unlock screen, per-IP/element rate
  limiting, shared-unlock by password, `no-store` + `noindex` on protected
  responses, CP-user bypass, and a configurable unlock template.
